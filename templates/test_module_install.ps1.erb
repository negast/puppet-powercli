if (Get-Module -ListAvailable -Name VMware.PowerCLI) {
    Write-Host "Module exists so return 1 to not install"
    { exit 1 }
} 
else {
    Write-Host "Module does not exist exit 0 to install"
    { exit 0 }
}